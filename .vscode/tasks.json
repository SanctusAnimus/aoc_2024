{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Project (MSVC)",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}/build",
                "--config",
                "Debug" // Specify Debug configuration for MSVC
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
